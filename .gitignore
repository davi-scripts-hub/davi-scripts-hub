IS_CUSTOM_UI_MODE = true
CUSTOM_UI_MODE_DATA = {
    Name = "Davi",
    Theme = "darkblue",
    Logo = "rbxassetid://96012102609548",
    Background = "rbxassetid://98096100327291",
    YouTube = "DaviScripts",
    Discord = "vh8hKNTc4",
    WelcomeMessage = {
        "Davi Scripts HUB",
        "BY Davi Scripts"
    },
    TrailIcon = "💙",
    ToggleIcon = "rbxassetid://88870369034630",
    CommunityIcon = "rbxassetid://129698128709234",
    Contributers = {
        "Davi Scripts | hashishx"
    },
    IntroText = "Acredite em si mesmo e você será imparável.",

    GetKeyLink = "https://direct-link.net/5038977/MyrmI39cr5La",
    TutorialLink = "https://direct-link.net/5038977/MyrmI39cr5La"
}

_G.MY_KEY_IS = "https://direct-link.net/5038977/MyrmI39cr5La"

loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/1c3b3c8a311a71c8e81c44b998a81e39.lua"))()Instance.new("UIListLayout", Frame)

OpenButton.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)

-- BOTÃO
function criarBotao(texto, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = Frame
    btn.Size = UDim2.new(1,0,0,35)
    btn.Text = texto
    btn.BackgroundColor3 = Color3.fromRGB(30,30,30)
    btn.TextColor3 = Color3.new(1,1,1)

    btn.MouseButton1Click:Connect(callback)
end

-- EQUIPAR ARMA
function Equipar()
    pcall(function()
        for i,v in pairs(player.Backpack:GetChildren()) do
            if FarmMode == "Melee" and v.ToolTip == "Melee" then
                char.Humanoid:EquipTool(v)
            elseif FarmMode == "Sword" and v.ToolTip == "Sword" then
                char.Humanoid:EquipTool(v)
            elseif FarmMode == "Gun" and v.ToolTip == "Gun" then
                char.Humanoid:EquipTool(v)
            elseif FarmMode == "Fruit" and v.ToolTip == "Blox Fruit" then
                char.Humanoid:EquipTool(v)
            end
        end
    end)
end

-- ATAQUE AUTOMÁTICO
function Attack()
    pcall(function()
        if char:FindFirstChildOfClass("Tool") then
    char:FindFirstChildOfClass("Tool"):Activate()
        end
    end)
end

-- FLY SYSTEM
local Fly = false
local BV, BG

function StartFly()
    pcall(function()
        Fly = true

        if not char:FindFirstChild("HumanoidRootPart") then return end

        BV = Instance.new("BodyVelocity")
        BV.MaxForce = Vector3.new(999999,999999,999999)
        BV.Velocity = Vector3.new(0,0,0)
        BV.Parent = char.HumanoidRootPart

        BG = Instance.new("BodyGyro")
        BG.MaxTorque = Vector3.new(999999,999999,999999)
        BG.CFrame = char.HumanoidRootPart.CFrame
        BG.Parent = char.HumanoidRootPart
    end)
end

function StopFly()
    Fly = false
    if BV then BV:Destroy() end
    if BG then BG:Destroy() end
end
-- AUTO FARM
function AutoFarm()
    while Toggles.Farm do
        task.wait()

        pcall(function()
            local enemiesFolder = workspace:FindFirstChild("Enemies")
            if not enemiesFolder then return end

            for i,v in pairs(enemiesFolder:GetChildren()) do
                if v:FindFirstChild("Humanoid") and v:FindFirstChild("HumanoidRootPart") then
                    
                    repeat task.wait()

                        if not Toggles.Farm then break end

                        Equipar()

                        char.HumanoidRootPart.CFrame =
                            v.HumanoidRootPart.CFrame * CFrame.new(0,5,0)

                        Attack()

                    until v.Humanoid.Health <= 0 or not Toggles.Farm
                end
            end
        end)
    end
end

-- AUTO CHEST OTIMIZADO
function AutoChest()
    while Toggles.Chest do
        task.wait(1)

        pcall(function()
            for i,v in pairs(workspace:GetDescendants()) do
                if string.find(v.Name:lower(),"chest") then
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        char.HumanoidRootPart.CFrame = v.CFrame
                        task.wait(0.3)
                    end
                end
            end
        end)
    end
end

-- BOTÕES
criarBotao("Auto Farm", function()
    Toggles.Farm = not Toggles.Farm
    if Toggles.Farm then task.spawn(AutoFarm) end
end)

criarBotao("Auto Chest", function()
    Toggles.Chest = not Toggles.Chest
    if Toggles.Chest then task.spawn(AutoChest) end
end)

criarBotao("Modo: Melee", function() FarmMode = "Melee" end)
criarBotao("Modo: Fruit", function() FarmMode = "Fruit" end)
criarBotao("Modo: Sword", function() FarmMode = "Sword" end)
criarBotao("Modo: Gun", function() FarmMode = "Gun" end)

criarBotao("Fechar", function()
    ScreenGui:Destroy()
end)
